[default]
# Configurações gerais do sistema
SOURCE_FILE_COLUMN_NAME = "SOURCE_FILE"
SHEET_NAME_COLUMN_NAME = "SHEET_NAME"

[default.default_sheet]
# Configurações da aba padrão a ser lida
name = ""  # Nome da aba padrão (deixe vazio para todas as abas)
sheet_candidates = ["LPU", "01"]  # Candidatos a nomes de abas
pattern_default = "default01"

[default.expected_columns.default01]
# Colunas esperadas para o padrão "default01"
columns = [
    "ID",  # Identificador único do item
    "DESCRIÇÃO",  # Nome do item
    "UN.",  # Unidade de medida
    "UNITÁRIO",  # Preço unitário
    "COMENTÁRIO",  # Comentários adicionais
    "QUANTIDADE",  # Quantidade do item
    "TOTAL"  # Valor total do item
]

[default.expected_columns.default02]
# Colunas esperadas para o padrão "default02"
columns = [
    "ITEM",  # ID da categoria
    "DESCRIÇÃO",  # Nome do item
    "UN",  # Unidade de medida
    "QUANT",  # Quantidade do item
    "PREÇO UNITÁRIO",  # Preço unitário
    "",  # Coluna vazia
    "PREÇO TOTAL"  # Valor total do item
]

[default.alternative_columns.default01]
# Colunas alternativas mínimas para o padrão "default01"
columns = [
    "ID",  # Identificador único do item
    "DESCRIÇÃO",  # Nome do item
    "UNITÁRIO",  # Preço unitário
    "QUANTIDADE"  # Quantidade do item
]

[default.alternative_columns.default02]
# Colunas alternativas mínimas para o padrão "default02"
columns = [
    "DESCRIÇÃO",  # Nome do item
    "QUANT.",  # Quantidade do item
    "P.U."  # Preço unitário
]

[default.default01.result]
# Rename de colunas para o padrão "default01"
dict_rename = {"ID" = "ID", "DESCRIÇÃO" = "NOME", "UN." = "UNIDADE", "UNITÁRIO" = "PREÇO LPU", "COMENTÁRIO" = "COMENTÁRIO", "QUANTIDADE" = "QUANTIDADE", "TOTAL" = "VALOR TOTAL"}

[default.default02.result]
# Rename de colunas para o padrão "default02"
dict_rename = {"ITEM" = "ID", "DESCRIÇÃO" = "NOME", "UN" = "UNIDADE", "QUANT" = "QUANTIDADE", "PREÇO UNITÁRIO" = "PREÇO LPU", "" = "", "PREÇO TOTAL" = "VALOR TOTAL"}

[default.result]
# Colunas esperadas no final do processo
list_result_columns = ["ID", "NOME", "UNIDADE", "PREÇO LPU", "COMENTÁRIO", "QUANTIDADE", "VALOR TOTAL", "SOURCE_FILE", "SHEET_NAME"]

[default.filtros]
# Filtros aplicados no pós-processamento
dict_filtros = {"QUANTIDADE" = "greater_than:0"}

[default.dir_outputs]
# Diretório de saída padrão
path = "outputs"  # Caminho para salvar os arquivos de saída